# Dev tooling
ledger.db
CLAUDE.md

# Archive — kept locally, not tracked
archive/

# Source data — stays local, not in repo
data/

# Generated — rebuild with build.sh / pack.sh
music-data-engine.zip

# Claude Code
.claude/

# inbox/ retired 2026-06-21 per ADR-0013 (two stores: backlog-tickets/ + dialog-log).
# Former rules (/inbox/* + !/inbox/processed/) removed with the dir.

# Runtime artifact — regenerated by every playlist run, binary diffs not useful in history
recommendation_log.db

# Python bytecode cache
__pycache__/
