# Findings canon — music_taste_engine

Last rendered: 2026-07-22 · current data snapshot `593629f6aa05` · generated by `registry.py` (do not hand-edit).

What we've learned, each claim traceable to the analysis, data snapshot, and
date that produced it. **STALE** = the finding ran on a snapshot other than the
one now on disk; re-run its analysis to refresh.

| status | finding | claim | analysis | ran | stale? |
|---|---|---|---|---|---|
| supported (high) | `engine.peak_hour_local` | Peak listening hour is ~8pm LOCAL (America/Chicago) with 15.4% late-night, not 2am / 42.8% as UTC-naive hours … | `engine_signals` | 2026-07-22 | ok |
| supported (high) | `self_affinity.self_replay` | Mark replays his OWN catalog heavily — 1,014 plays across 147 distinct self-recorded tracks (artist 'Mark Kora… | `self_affinity` | 2026-07-22 | ok |
| provisional (low) | `mte006.playlist_fatigue` | No detectable playlist-driven fatigue: plays corroborated as part of a playlist-order run are skipped LESS (~0… | `mte006_playlist_fatigue` | 2026-07-22 | ok |
| untestable (high) | `mte005.context_convergence` | Whether repeat use of a mood/context converges on the same tracks is UNTESTABLE as instrumented: 5 logged runs… | `mte005_context_analysis` | 2026-07-22 | ok |

## Caveats

- `engine.peak_hour_local` — Requires tzdata on Windows for historically-correct DST; falls back to UTC with a warning if absent.
- `mte005.context_convergence` — Blocked by mte-007: recommendation_log needs populated run timestamps + an outcome/feedback channel before convergence is measurable.
- `mte006.playlist_fatigue` — Temporal validity is a large filter — only the strict-window subset of corroborated plays matches a playlist order recorded near the play date. A weak per-track 'skipped-more-in-its-playlist' signal exists but is too thin (n=3–9) to confirm cumulative overplay. Direct causal test is impossible: Spotify's export has no playlist attribution (no context_uri).
- `self_affinity.self_replay` — Consumption-side only; content-feature output↔taste alignment (mte-001) is a separate, undecided axis.
